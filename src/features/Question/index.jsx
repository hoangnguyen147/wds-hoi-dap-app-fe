import React from 'react';
import PropTypes from 'prop-types';

Questions.propTypes = {
    
};

function Questions(props) {
    return (
        <div>
            Question list in here
        </div>
    );
}

export default Questions;